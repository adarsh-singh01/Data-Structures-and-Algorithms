#include<iostream>//preprocessor directive
// #include<bits/stdc++.h> //includes all standard libraries but increases compilation time
//using namespace std; //std is standard library which has cout and cin
//if we use this line we dont have to write std:: before cout and cin

int main()//main function is starting point of program if we dont write it program will not run and give error
{
    std::cout << "Hello, World!"<<"\n"<<"my first prog"<<"\n";
    std::cout << "This project uses the C++17 language standard version."<<std::endl;
    std::cout<<"adarsh";
    return 0;//signal to operating system that program ended successfully if we dont write it still it will return 0 but its good practice to write it
}