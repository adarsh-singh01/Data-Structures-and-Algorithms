/*
PATTERNS MEANS NESTED LOOPS

Rules to print any kind of pattern:

1. For the outer loop, count the number of rows/lines.
2. For the inner loop, count the number of columns/characters in each line. and connect them somehow to the rows.
3. print the "*" in the inner for loop.
4. Observe symmetry (optional).

*/ 